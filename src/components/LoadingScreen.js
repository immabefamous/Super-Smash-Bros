const LoadingScreen = ({selectedCharacter, loadingScreenVisible}) => {
    return(
        <div style={{ display: loadingScreenVisible }}className="loading-screen">
        
        </div>
    )
}

export default LoadingScreen;